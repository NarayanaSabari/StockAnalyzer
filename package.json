{
  "name": "StockAna",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "concurrently --names \"NODE,VITE\" -c \"cyan,green\" \"cd server && npm install\" \"cd client && npm install\"",
    "dev": "concurrently --names \"NODE,VITE\" -c \"cyan,green\" \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "format": "prettier --write \"**/*.{md,js,jsx,cjs,json,css,html}\""
  },
  "author": "narayanasabari",
  "license": "ISC",
  "description": "",
  "devDependencies": {
        "concurrently": "^8.2.2",
        "prettier": "^3.2.5",
        "prettier-plugin-tailwindcss": "^0.5.12"
  },
  "dependencies": {
        "@ianvs/prettier-plugin-sort-imports": "^4.2.1"
  }
}
